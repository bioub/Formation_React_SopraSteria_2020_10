// Une expression en JS, c'est :
// - tout ce qui a une valeur
// - tout ce qu'on peut mettre dans une variable
// - tout ce qu'on peut passer en paramètre

console.log('Jean'); // 'Jean' est une expression
console.log('Jean'.toUpperCase()); // 'Jean'.toUpperCase() est une expression

console.log(1 + 1); // 1 + 1 est une expression

setInterval(() => {}, 1000); // () => {} et 1000 sont des expressions,
